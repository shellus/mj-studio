# 模型选择器组件设计文档

## 背景

当前项目中有两处模型选择逻辑：
- **对话页面** (`MessageInput.vue`)：选择对话模型
- **绘图页面** (`Workbench.vue`)：选择绘图模型

两者存在大量重复代码，需要统一为一个组件。

## 设计方案

创建统一的 `ModelSelector` 组件，通过 `category` prop 区分对话/绘图模型。

### 展示形式

单层下拉菜单，按上游分组展示所有模型：

```
┌─────────────────────────────────┐
│ 🔲 上游A / gpt-4o           ▼  │  ← 触发按钮，显示当前选中
└─────────────────────────────────┘
        ↓ 点击展开
┌─────────────────────────────────┐
│ 上游A                      ⚙️  │  ← 分组标题 + 设置按钮
│   ├ ✨ gpt-4o                   │
│   └ ✨ gpt-4o-mini              │
├─────────────────────────────────┤
│ 上游B                      ⚙️  │
│   ├ 🎨 claude-3-opus            │
│   └ 🎨 claude-3-sonnet          │
└─────────────────────────────────┘
```

- 上游名作为分组标题（不可点击），旁边有设置按钮跳转到配置页
- 模型名作为可选项，带图标，点击选中

### 组件 API

```typescript
// Props
interface Props {
  modelConfigs: ModelConfig[]
  category: 'chat' | 'image'
  configId?: number | null
  modelName?: string | null
}

// Emits
interface Emits {
  'update:configId': [id: number | null]
  'update:modelName': [name: string | null]
}

// Expose
interface Expose {
  selectedConfig: ModelConfig | undefined
  selectedModelTypeConfig: ModelTypeConfig | undefined
}
```

### 使用示例

```vue
<!-- 对话页面 -->
<ModelSelector
  :model-configs="modelConfigs"
  category="chat"
  v-model:config-id="currentConfigId"
  v-model:model-name="currentModelName"
/>

<!-- 绘图页面 -->
<ModelSelector
  ref="selectorRef"
  :model-configs="modelConfigs"
  category="image"
  v-model:config-id="selectedConfigId"
  v-model:model-name="selectedModelName"
/>
```

## 实施步骤

1. 创建 `app/components/ModelSelector.vue`
2. 修改 `MessageInput.vue` 使用新组件
3. 修改 `Workbench.vue` 使用新组件
4. 测试两个页面功能

## 文件变更

| 文件 | 操作 |
|------|------|
| `app/components/ModelSelector.vue` | 新建 |
| `app/components/chat/MessageInput.vue` | 修改 |
| `app/components/drawing/Workbench.vue` | 修改 |
